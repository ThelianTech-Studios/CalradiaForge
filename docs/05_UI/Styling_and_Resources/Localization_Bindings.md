# Localization Bindings

Localized UI properties are supplied by the injected translation service and
refreshed when the active language changes. ViewModels keep fallback values
available through the Core translation contract; generated language JSON is a
resource artifact and is not a second runtime authority.

The key/resource owner is [Core Localization](../../02_Systems/CalradiaForge_Core/Localization/README.md).
