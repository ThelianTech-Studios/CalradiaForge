<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<!--Template for Title Bar Close Button -->
	<ControlTemplate x:Key="TitleBarCloseButtonTemplate" TargetType="Button">
		<Border x:Name="ButtonBorder" Background="Transparent" BorderThickness="0"
						Padding="{TemplateBinding Padding}"
						CornerRadius="6" Margin="1,3"
						SnapsToDevicePixels="True">
			<Path x:Name="IconPath" Width="35" Height="35"
						HorizontalAlignment="Center" VerticalAlignment="Center"
						Data="M3,3 L11,11 M11,3 L3,11"
						Stroke="{StaticResource TB.Txt.Bright}"
						StrokeThickness="3" Stretch="Uniform"
						StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
		</Border>
		<ControlTemplate.Triggers>
			<Trigger Property="IsMouseOver" Value="True">
				<Setter TargetName="IconPath" Property="Stroke" Value="{StaticResource TB.Acc.Red}" />
				<Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource TB.Sfc.Light}" />
			</Trigger>
			<Trigger Property="IsPressed" Value="True">
				<Setter TargetName="ButtonBorder" Property="Opacity" Value="0.85" />
			</Trigger>
		</ControlTemplate.Triggers>
	</ControlTemplate>

	<!--Template for Title Bar Minimize Button -->
	<ControlTemplate x:Key="TitleBarMinimizeButtonTemplate" TargetType="Button">
		<Border x:Name="ButtonBorder" Background="Transparent" BorderThickness="0"
						Padding="{TemplateBinding Padding}"
						CornerRadius="6" Margin="1,3"
						SnapsToDevicePixels="True">
			<Path x:Name="IconPath" Width="40"
						HorizontalAlignment="Center" VerticalAlignment="Center"
						Data="M2,8 L12,8"
						Stroke="{StaticResource TB.Txt.Bright}"
						StrokeThickness="3" Stretch="Uniform"
						StrokeStartLineCap="Round" StrokeEndLineCap="Round">
				<Path.RenderTransform>
					<TranslateTransform Y="15" />
				</Path.RenderTransform>
			</Path>
		</Border>
		<ControlTemplate.Triggers>
			<Trigger Property="IsMouseOver" Value="True">
				<Setter TargetName="IconPath" Property="Stroke" Value="{StaticResource TB.Acc.Gold}" />
				<Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource TB.Sfc.MedLight}" />
			</Trigger>
			<Trigger Property="IsPressed" Value="True">
				<Setter TargetName="ButtonBorder" Property="Opacity" Value="0.85" />
			</Trigger>
		</ControlTemplate.Triggers>
	</ControlTemplate>

	<!--Custom Styles for Title Bar Buttons -->
	<Style x:Key="TitleBarCloseButton" TargetType="Button">
		<Setter Property="Template" Value="{StaticResource TitleBarCloseButtonTemplate}" />
		<Setter Property="Padding" Value="5,0" />
		<Setter Property="Width" Value="50" />
		<Setter Property="Height" Value="50" />
		<Setter Property="VerticalAlignment" Value="Center" />
		<Setter Property="HorizontalAlignment" Value="Center" />
		<Setter Property="Cursor" Value="Hand" />
	</Style>
	<Style x:Key="TitleBarMinimizeButton" TargetType="Button">
		<Setter Property="Template" Value="{StaticResource TitleBarMinimizeButtonTemplate}" />
		<Setter Property="Padding" Value="5,0" />
		<Setter Property="Width" Value="50" />
		<Setter Property="Height" Value="50" />
		<Setter Property="VerticalAlignment" Value="Center" />
		<Setter Property="HorizontalAlignment" Value="Center" />
		<Setter Property="Cursor" Value="Hand" />
	</Style>

	<!--End of TitleBar Resources-->
</ResourceDictionary>